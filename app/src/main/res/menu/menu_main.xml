<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/mainMenuSearch"
        android:icon="@drawable/ic_search"
        app:showAsAction="ifRoom"
        android:title="@string/menu_search_title"
        android:tooltipText="@string/menu_search_tooltip" />

    <item
        android:id="@+id/mainMenuUser"
        android:icon="@drawable/ic_account"
        app:showAsAction="ifRoom"
        android:title="@string/menu_account_title"
        android:tooltipText="@string/menu_account_tooltip" />

</menu>